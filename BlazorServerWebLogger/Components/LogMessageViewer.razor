@using BusinessEntity.WebLogger.Models
@using BlazorServerWebLogger.Components
<div class="main-logger-logEntry">
    <!-- Верхняя часть сообщения с основной информацией -->
@*     <div class="log-entry-header">
        <div class="log-entry-data-line">@LogEntry?.Timestamp.ToString("dd.MM.yyyy HH:mm:ss")</div>
        <div class="log-entry-data-line">@LogEntry?.ServiceCode</div>
        <div class="log-entry-data-line">@LogEntry?.MessageType</div>
    </div> *@

    <!-- Многострочное текстовое поле для сообщения -->
    <div class="log-entry-message">
        @LogEntry?.Message
    </div>
</div>

@code {
    [Parameter]
    public LogEntryDbStorable LogEntry { get; set; }
}